TASK: Visual Studio 솔루션 스캐폴드

참고 문서:
- docs/PRD.md
- docs/Architecture.md
- docs/agent.md

우선순위:
- PRD > Architecture > 본 태스크

목표:
- 아키텍처에 맞춰 Visual Studio 솔루션과 빈 프로젝트를 만든다.
- 비즈니스 로직은 구현하지 않는다.

솔루션:
- 이름: VssActivityMonitor.sln

프로젝트 생성:
1) VssMon.Core
   - 타입: Static Library
   - 언어: C++
   - 위치: src/core/
   - Unicode 사용
   - MFC 의존성 없음

2) VssMon.UiMfc
   - 타입: Windows Desktop Application (MFC)
   - 위치: src/ui_mfc/
   - Unicode 사용
   - 동적 MFC DLL 사용

3) VssMon.Tests
   - 타입: Console Application
   - 위치: src/tests/
   - GoogleTest 준비(구현은 미필수)

구조 규칙:
- Core 헤더: src/core/include/vssmon/
- Core 소스: src/core/src/
- UI 헤더/소스: src/ui_mfc/ui/
- 코드 생성은 src/ 이하에만 둔다.

빌드 설정:
- 플랫폼: x64
- 구성: Debug, Release
- 툴셋: MSVC v143 이상
- C++ 표준: C++20(Core는 C++17 호환)

콘텐츠 규칙:
- ETW, UI 로직, 상관분석 미구현.
- 필요한 최소의 placeholder cpp/h만 추가.
- Release x64 빌드가 성공해야 한다.

완료 조건:
- VssActivityMonitor.sln이 Release x64에서 빌드 성공.
- 경고를 에러로 취급하지 않는다.
- 커밋 메시지: "chore: scaffold Visual Studio solution and projects"
